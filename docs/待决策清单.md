# 待决策清单

> 这份是**全部**需要你判断的事项，一次性列全，不再零散地抛。
> 每项都给了我的建议，你只需要在「你的决定」一栏写几个字。
> 没有列进来的，都是我能自己判断或已经做掉的。

生成时间：2026-09-25　对应构建：v0.1.0（护栏 6/6，退出码 0）

---

## 一、最该你过目的（判断量最大，影响面最广）

### 1. 重映射对照表：我做的合并对不对

文件：`tests/label-remap-review.txt`

我把原来的 **278 个文言味细标签**（`尾大不掉`、`纳士`、`兔死狗烹`…，其中 106 个只出现过一次）
并成了 **75 个现代汉语标签**（`组织膨胀`、`招才`、`功高震主`…）。**每一个合并都是我的语义判断**，
可能有归错的。198 条史料的对照表已生成。

**你的决定**：______________________（例如「抽看过前 50 条，没问题」或「第 X 条不该并进去」）

---

## 二、期望值：需要你读那三条史料标题，判断贴不贴题

判定方式是「want 里任意一项出现在 Top3 即通过」。现在 6 条全过，但**过的方式有强弱**：

| 用例 | 现状 Top3 | 我的看法 |
| --- | --- | --- |
| Q1 合伙人猜忌/挑拨/骨干想走 | **鸿门宴：错失唯一时机** / 陈平反间 / 将相和 | #1 我认为**不贴题**（鸿门宴讲的是错失时机，不是内部猜忌）。#2 陈平反间才是正解 |
| Q3 扩张太快/管理失控/各自为政 | **八王之乱** / 大业年间的超负荷工程 / 考成法与一条鞭法 | 三条我都认为切题。八王之乱正是"诸侯各自为政"的典型。排序偏好问题 |
| Q6 团队矛盾很深/互不信任 | 将相和 / **巨鹿破釜沉舟** / **鸿门宴** | #1 正确。#2#3 我认为**不贴题**（破釜沉舟是孤注一掷、鸿门宴是错失时机，都不是团队矛盾） |
| Q2 / Q4 | 见 `tests/cases.json` | 已标 `snapshot`（变更探测器），不代表唯一正解。Q2 刚换过一版更贴题的 |

**你的决定**：
- Q1 的 #1（鸿门宴）你接受吗？ → ______________________
- Q6 的 #2#3 你接受吗？ → ______________________
- 若要改，是改期望值还是让我调排序？ → ______________________

---

## 三、机制取舍（都是可调的旋钮）

### 2. `synonyms.json` 删不删 —— **已删（按你的决定）**

内容 350/351 并进了 `labels.json`（`诸吕` 按你决定删）。仓库里另有改动前的完整备份。

### 3. 命中饱和系数（我定的是 0.3）

得分 = 最高权重的命中词 + **0.3** × 其余命中词之和。
不用"除以命中词数"是因为那会反向惩罚覆盖更全的文档。

**你的决定**：保持 0.3 / 改成 ___ → ______________________

### 4. 停用词表 72 个要不要过一遍

我只能保证"没把有实义的词收进去"（`不利`/`不足`/`不如`/`不能` 都刻意没收），
**不能保证"该收的都收了"**。文件：`resources/stopwords.json`。

**你的决定**：______________________（例如「不用看」或「我加几个」）

---

## 三之补：2 字 trigger 的护栏（新发现，2026-09-25 追加）

**现状**：非标签名的 trigger 必须 ≥3 字才可能触发。实测这条规则挡住了 **209 个 2 字 trigger**，
其中约 150 个是真会说的词（谣言 / 流言 / 对赌 / 士气 / 决策 / 孤立 / 退路 / 反间 / 隐忍 / 军心 / 训练 / 情报 …）。

**但不能整条放宽**：实测查询里的「团队」会触发【用人】标签，把 Q3 期望的《考成法与一条鞭法》挤出前三。
`团队` 和 `谣言` 都是 2 字，df 也都不高（9 / 0），**用频次区分不了**。

| 选项 | 做法 | 代价 |
| --- | --- | --- |
| A | 保持现状 | 那 150 个词要改写成 3 字以上才生效（`谣言` → `散布谣言`） |
| **B（我的建议）** | 新增 `short_triggers` 字段，作者显式标出安全的 2 字词，只放行这些 | 多一个字段，但显式可审计，不会因泛词漏网而误触发 |
| C | 全放开 + 手工黑名单（团队/士气/决策/情报…） | 黑名单要长期维护，漏一个就误触发 |

**你的决定**：______________________

> 附带一件我可以直接做的（不需要你决定）：把所有被挡的 2 字 trigger 补成 3 字以上的说法。

---

## 四、语料侧：6 个标签没有史料挂靠，要补史料

```
培养人   功成身退   退出机制   反目   风险管理   外交
```

网络不通，源仓库（`alephpi/24histories-simplified-chinese`）拉不到，所以只能**我按二十四史自己写条目**
（原文 + 出处 + 译文 + 决策/结果）。新增条目会单独标注，不混进原有 198 条。

**你的决定**：
- 要不要我做这批条目？ → ______________________
- 新增条目的**原文准确性由谁负责核**？（我写的东西需要你或另一个人验，否则等于给产品埋了假史料） → ______________________

---

## 五、我自己做掉的决定（你可能有异议，先摆出来）

这些我按判断做了，没问你。你若不同意，说一声我改：

| 我做的事 | 理由 |
| --- | --- |
| 史料**两层都挂**（粗标签 + 保留原有细 themes） | 只挂粗标签时，`将相和` 从"团队矛盾"Top3 里整个消失了 —— 细标签 `将帅不和`(df=3) 被并成 `内部矛盾`(df=46)，区分度丢了。粗层负责"对得上"，细层负责"分得开" |
| 归一方式选「饱和」而不是「除以命中词数」 | 后者会反向惩罚覆盖更全的文档 |
| 标签名一律 ≤4 字（硬约束） | 索引最长只发 4 字词元，超了标签名自己都进不了索引，等于白建。为此把 `合伙人反目`→`反目`、`改革推不动`→`改革受阻` |
| 撤回了三个停用词（各地/各自/两年） | 它们是**因为 Q3 不过**才被我加进去的，触发我加的不是词本身而是测试 —— 你已经指出，已撤 |
| 决策分析栏的两栏叫「这么做的好处 / 这么做要付的代价」，程序不写结论 | 你要的是 pros 和 cons，不提「建议」。正文随语料离线写好，运行时不生成任何文字 |

### 5.1 决策分析栏（你新加的那个功能）—— 已实现，形态如下

每条结果最后多一块：

* **左栏「这么做的好处」、右栏「这么做要付的代价」** —— 正文是**逐条史料离线写好的**
  （语料里新增 `pros` / `cons` 两个字段）。201 条全部写完：564 条 pros、611 条 cons，
  每条 2–3 句，只依据该条自带的原文/译文/决策/结果，不引入史料外的事实。
* **一行「与你的处境对应」** —— 把这条史料带的情境标签中**原样出现在你输入里**的那几个
  标出来（打勾）。这是运行时唯一现算的部分，纯子串比对，不涉及生成。
* 最后一句固定写明「程序不给结论，哪一边更重取决于你的实际情况」。

**几处需要你过目的**：
- 写法口径是「pros = 这个决策在什么条件下成立、当事人得到了什么；cons = 付出了什么代价、
  依赖什么前提、什么情况下会反过来失败」。所以**反面决策**（土木堡之变、鸿门宴这类）
  的 pros 读起来会像「他当时图什么」而不是「这步走对了」。你若想换个口径，说一声。
- 每条的 pros/cons 是**我的判断**，不是史料原文里写着的。审校请重点看有没有把
  「后人的评价」当成「当事人的处境」来写。

---

## 六、我还没做、且不需要你决定的（我的 TODO）

列出来让你知道进度，不用你回复：

- [x] `tests/DataCheck.cs` 改读 `labels.json`（`synFile["groups"]` 也一并改成 `["labels"]`，那正是它抛 NullReferenceException 的原因）
- [x] README 里同义词表那一节改成标签表 + 停用词表，并把过期的数字（198 条 / 44,256 词）更新为 201 条 / 45,416 词
- [x] 桌面那份副本已更新到最新构建（201 条、带 `pros`/`cons` 与决策分析栏）
- [x] `docs/更新服务托管说明.md` 的 `data_version.json` 示例补上 `files` 区段（labels.json / stopwords.json）
- [x] `tests/AssetCheck.cs` 的 URL 清单同步为当前内存资源（它此前在找已删除的 jigu.js / rules.js / rules.json / synonyms.json，必然失败）
- [x] `tools/make-preview.ps1` 修好：它此前读四个已删除的文件，且硬编码 `D:\DSH\jigu-app`；现在改为 `$PSScriptRoot` 推导，并喂给离线引擎真实语料
